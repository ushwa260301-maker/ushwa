# AUDIT_PROTOCOL — 코드 감사 절차 · 감사자 역할

> 이 문서는 감사 **방법**을 정한다. 감사 **결과**는 `AUDIT.md` 에 남긴다.
> 둘을 한 문서에 섞지 않는다 — 방법은 오래 가고 결과는 시점에 묶인다.
>
> 근거: 2026-09-23 `species-catalog` baseline audit (base `584ca21`) 에서
> 실제로 발생한 문제로부터 도출했다. 추측으로 만든 규칙은 없다.
> 확정되지 않은 것은 `[확인 필요]`.

## 0. 감사자의 역할 (확정)

- 감사자는 **읽고 · 검증하고 · 기록한다.** 감사 턴에서 코드를 고치지 않는다.
- **이유**: 감사자가 감사 도중 코드를 고치면 Evidence 가 감사 중에 변한다.
  기준 커밋(§1)이 가리키는 상태와 기록된 Evidence 가 어긋나면 감사 기록
  전체가 검증 불가가 된다.
- 수정은 감사 종료 후 **별도 작업**으로, AUDIT 항목 ID를 참조해 수행한다.
- 감사 턴의 산출물: `AUDIT.md` 항목 + **변경된 코드 0줄**. 감사 턴을 마칠
  때 변경 파일이 없음을 명시한다.
- 감사자는 발견을 **축소하지도 과장하지도 않는다.** 확인된 것은 확인됐다고,
  추정인 것은 추정이라고 쓴다.

## 1. 감사 기준 커밋 고정 (확정)

모든 baseline audit 은 문서 최상단에 다음 4개를 기록한다.

```
Audit Date   : YYYY-MM-DD
Audit Base   : <감사한 기준 커밋 SHA>
Scope        : <실제로 읽은 영역>
Out of Scope : <읽지 않은 영역>
```

`Audit Base` 가 없으면 `file:line` 형태의 Evidence 는 **코드가 한 번만
바뀌어도 검증 불가**가 된다. 기준 커밋은 선택이 아니라 Evidence 의 전제다.

### 동결 필드 / 갱신 필드

| 필드 | 성격 | 규칙 |
|---|---|---|
| `Evidence` | 감사 당시 사실의 스냅샷 | **동결** — 줄 번호가 밀려도 수정하지 않는다 |
| `Impact` | 그 사실의 결과 판단 | **동결** |
| `Status` | 진행 상태 | 갱신 |
| `Required Action` | 해야 할 일 | 갱신 |
| `Dependency` | 선행 항목 | 갱신 |
| `Verification` | 증명 방법·결과 | 갱신 |

- 기준선(Evidence/Impact)과 추적(Status/Verification)을 한 필드에 섞으면
  "baseline audit" 과 "진행 추적" 이 서로를 무효화한다. 필드로 분리한다.
- Evidence 가 틀린 것으로 밝혀지면 **수정하지 않고** 항목 하단에
  `Correction:` 을 덧붙인다. 감사 기록은 덮어쓰지 않고 누적한다.

### Evidence 기록 형식

`file:line` 만으로는 기준 커밋을 체크아웃해야 대조가 된다. **해당 시점의
코드를 짧게 함께 인용**해 문서만으로 대조가 되게 한다.

```
Evidence: cloudStore.js:441 — `engine_version: dbg.model || ""`
          schema.sql:124   — 주석 "engine_version = vision.js 커밋 SHA"
```

## 2. DEFECT 와 DECISION 을 구분 (확정)

모든 감사 항목을 같은 상태로 관리하지 않는다.

| 종류 | 정의 | 상태 전이 |
|---|---|---|
| **DEFECT** | 코드·설계상의 실제 결함 | `OPEN → FIXED → VERIFIED` |
| **DECISION** | 결함인지 여부 또는 방향을 정해야 하는 사안 | `OPEN → DECIDED` |

- **DECISION 을 DEFECT 상태 머신에 넣으면 영원히 OPEN 으로 남는다.**
  고칠 대상이 아니라 정할 대상이기 때문이다.
- DECISION 이 `DECIDED` 된 뒤 구현이 필요하면 **별도 DEFECT 항목을 신규
  생성**한다. DECISION 항목을 구현 항목으로 전용하지 않는다.
- `VERIFIED` 후 같은 결함이 재발하면 `REGRESSED` 로 표시하고 원인을
  기록한다 — 조용히 `OPEN` 으로 되돌리지 않는다. 재발 사실 자체가 정보다.
- 고치지 않기로 한 DEFECT 는 `WONTFIX` + 사유. 상태를 비워두지 않는다.

### 분류 예 (2026-09-23 감사)

| 항목 | 분류 | 사유 |
|---|---|---|
| `engine_version` 주석/구현 불일치 | **DEFECT** | 설계 의도가 구현되지 않음 |
| OCR 99% 목표 | **DECISION** | 목표 재정의 — 고칠 결함이 아님 |
| RLS 전원 DELETE 권한 | **DECISION** | VISION "데이터는 공용" 원칙상 의도된 설계일 수 있음 — 먼저 판정 |
| 두 프로젝트 공존 | **DECISION** | 분리 여부는 별도 판단 |
| 공개 저장소 PII 노출 | **DEFECT** | 명백한 결함 |

## 3. Scope 를 정직하게 표기 (확정)

현재 저장소에는 **두 개의 프로젝트**가 있다.

| 영역 | 프로젝트 |
|---|---|
| `species-catalog/` | 조경 거래명세서 OCR 플랫폼 (루트 문서 12종이 다루는 대상) |
| `backend/` `frontend/` `mobile/` `shared/` | 별도 프로젝트로 보이는 `어서화` 관련 영역 |

- 실제로 읽지 않은 영역을 감사 범위에 포함시키지 않는다.
- **"저장소 전체 감사 완료" 라고 표현하지 않는다.** 읽은 것만 읽었다고 쓴다.
- Out of Scope 영역에는 다음을 명시한다:
  > 본 감사에서 코드 감사하지 않았으며, 본 감사의 결론을 해당 영역에
  > 적용하지 않는다.
- 해당 영역의 감사가 필요해지면 **별도 Audit Scope 와 별도 기준 커밋**으로
  새 감사를 연다. 기존 감사 문서를 확장하지 않는다.

## 4. 개인정보 감사 원칙 (확정)

### 감사 문서에 PII 값을 복제하지 않는다

실물 거래명세서 기반 fixture 를 감사·비식별화할 때, 다음을 **감사 문서에
그대로 기록하지 않는다**: 실명 · 계좌번호 · 전화번호 · 주소 · 기타 제3자
식별정보.

**값이 아니라 유형과 위치만 기록한다:**

```
Evidence: tests/ocr-corpus/<n>-*.json — 제3자 실명 · 금융정보 · 휴대폰번호 포함
          (값은 기록하지 않음. 대조는 Audit Base 체크아웃으로 수행)
```

**이유**: 감사 문서에 PII 를 적으면 그 문서가 스스로 PII 정리 대상이 된다.
히스토리 정리 범위가 감사할 때마다 늘어난다.

### 비식별화는 corpus 전체 일관성으로 검증한다

- fixture 는 **원문(`ocr`)과 정답(`expect`)이 쌍**이다. 한쪽만 바꾸면 테스트가
  조용히 무의미해진다.
- 같은 이름·전화번호·상호가 **여러 fixture 에 교차 참조**될 수 있다. 개별
  파일만 고치지 말고 corpus 전수로 치환 대상을 먼저 확정한다.
- 비식별화 후 **OCR 실패 패턴과 parser 검증 의미가 보존되는지** 확인한다.
  예: 상호를 `예금주: 이름(상호)` 괄호 패턴에서 추출하는 fixture 는 더미로
  바꿔도 **그 괄호 구조가 유지돼야** 한다.
- 검증은 회귀 러너로 한다. **비식별화 전에 CI 가 먼저 동작해야 한다**(§6).
  안전망 없이 회귀 테스트의 입력을 고치지 않는다.

### 선행 조건

PII 를 가리키는 감사 문서는 **저장소가 비공개임을 확인한 뒤** 커밋한다.
공개 상태에서 올리면 노출을 줄이는 게 아니라 안내판을 세우는 것이다.
확인은 자가 보고가 아니라 **API 조회 등 실제 상태 조회**로 한다(§7 원칙의
감사 절차 자체에 대한 적용).

## 5. PII 차단은 문서가 아니라 CI 에서 (확정)

- fixture 원칙을 문서에 적는 것만으로는 재발을 막지 못한다. **사람이
  기억해야 하는 규칙은 언젠가 깨진다.**
- CI 에 **PII 패턴 가드**를 둔다. 프로젝트가 정의한 민감 패턴(계좌번호 ·
  휴대폰번호 등)을 검사하고, 발견되면 **PR 을 실패시킨다.**
- **GitHub secret scanning 에 의존하지 않는다.** 그것은 API 키·토큰 탐지용
  이며 계좌번호·실명은 잡지 않는다.
- 가드는 탐지 결과를 로그에 **값으로 출력하지 않는다** — CI 로그가 새로운
  노출 경로가 된다. 파일명과 패턴 종류만 보고한다.

## 6. CI 는 테스트의 성격을 구분한다 (확정)

`tests/*.mjs` 를 전부 테스트로 취급하지 않는다. **exit code 기반 회귀
테스트만 자동 게이트**로 쓴다.

### 게이트 대상 (5개)

| 러너 | 판정 근거 |
|---|---|
| `ocr-accuracy` | `process.exit(rate >= 0.95 ? 0 : 1)` |
| `guide-validate` | `process.exit(errors.length ? 1 : 0)` |
| `sync-pending` | exit code 기반 |
| `supplier-matcher` | exit code 기반 |
| `service-items` | exit code 기반 |

### 게이트 제외 (3개)

| 러너 | 제외 사유 |
|---|---|
| `cloud-smoke` | 외부 Supabase 상태에 의존 — 별도 수동/비게이트 검증 |
| `import-fixture` | `process.argv` 를 받는 **CLI 도구**, 테스트가 아님 |
| `classify-failure` | exit code 없는 **진단 도구** — `ocr-accuracy` 가 호출 |

### 실행 환경 규칙

- **`npm ci` 를 무조건 실행하지 않는다.** 루트 `package.json` 의 workspaces 는
  `species-catalog` 와 무관한 영역을 가리킨다. `species-catalog` 는 빌드·
  의존성이 없는 정적 사이트이며 러너는 Node 내장 모듈만 쓴다.
- `setup-node` + `node` 직접 실행으로 구성한다.
- **PR 및 feature branch 에서도 실행되어야 한다.** 배포 워크플로가 main
  전용인 것은 environment 보호 규칙 때문이며, 그 제약은 배포 잡에만
  해당한다. 테스트가 feature branch 에서 돌지 않으면 "CI PASS 확인" 이
  검증하는 대상이 없다.

## 7. Verification 을 필수화 (확정)

- 감사에서 발견한 결함은 **"수정했다" 로 종료하지 않는다.**
- 각 항목에 **무엇으로 증명하는지**를 기록한다. 증거 없는 `VERIFIED` 는 금지.

| 항목 유형 | Verification 예 |
|---|---|
| CI 부재 | PR 에서 해당 workflow green |
| PII 노출 | 정의된 PII 패턴 검사 결과 **0건** |
| `guide_id` 손실 | Cloud 왕복 후 `guide_id` 보존 확인 |
| Conflict 고착 | 충돌 발생 → 사용자 해결 → pending 제거 → Cloud 재조회까지 **통합** 검증 |
| provenance | 수정 후 **새로 생성된** 데이터에 정확한 버전 식별자 저장 확인 |

- Verification 은 **재실행 가능한 형태**로 적는다(커맨드 · 확인 절차).
  "확인함" 같은 서술은 Verification 이 아니다.
- 소급 불가한 항목(이미 쌓인 데이터 등)은 그 사실을 함께 기록한다.

## 8. provenance 를 특별히 추적 (확정)

OCR 데이터는 이 프로젝트의 핵심 자산이며(VISION §2), 그 분석 가치는
**"어떤 엔진·버전이 생성했는가" 를 구분할 수 있을 때만** 성립한다.

- `engine_version` 은 **실제 실행된 OCR 엔진 버전을 구분**할 수 있어야 한다.
  고정 문자열은 구분자 역할을 하지 못한다.
- schema 주석과 실제 구현이 다르면 **별도 DEFECT 로 기록**한다. 주석을
  구현으로 간주하지 않는다.
- **`DEVELOPMENT_RULES.md` §6 의 "OCR Engine 변경 시 A/B Test" 규칙은
  provenance 가 확보돼야 실행 가능하다.** baseline 과 candidate 가 데이터
  상에서 구분되지 않으면 A/B 비교의 근거가 남지 않는다. 엔진·전처리 개선
  작업은 provenance 수정을 선행 조건으로 둔다.
- 수정 후 검증은 **새로 생성되는 데이터** 기준이다. 이미 쌓인 행은 소급
  되지 않으므로, **데이터가 더 쌓이기 전에 처리할수록 손실이 작다.**

## 9. main 도달 경로까지 감사 (확정)

feature branch 수정을 완료로 판단하지 않는다. 다음 7단계를 구분한다.

```
① 코드 수정 → ② 로컬 검증 → ③ CI green → ④ PR 생성
→ ⑤ 사용자 승인 → ⑥ main merge → ⑦ 배포 확인
```

- **`Status` 에는 현재 몇 단계인지를 적는다.** "수정됨" 은 ①일 뿐이다.
- P0 · P2 관련 변경은 **③ CI green 확인 후 ⑤ 사용자 승인**을 거쳐 main 에
  반영한다.
- 배포는 main push 에서만 일어나므로, **main 에 도달하지 않은 수정은
  운영에 반영되지 않았다.** 이 상태를 "완료" 로 적지 않는다.

## 10. 감사자의 핵심 원칙 (확정)

### 문서를 그대로 믿지 않는다

문서와 코드가 다르면:

1. **코드의 실제 상태**를 Evidence 로 기록한다
2. **문서 드리프트를 별도 항목**으로 기록한다
3. 문서를 코드에 맞게 갱신할 필요가 있으면 `Required Action` 으로 남긴다

주석·문서·이전 감사 결과 모두 **검증 대상**이지 근거가 아니다.
(2026-09-23 감사에서 `ROADMAP.md` 의 T5·T6·T7·T9 상태 4건이 실제 코드와
달랐고, `schema.sql` 주석이 구현과 달랐다.)

### 외부 제안도 코드로 검증한 뒤 채택한다

사람·다른 AI·이전 감사자의 제안을 그대로 실행하지 않는다. 제안이 전제하는
사실을 **코드에서 확인한 뒤** 채택·수정·기각한다. 기각한 경우 사유를 남긴다.

### "완료" 는 증거가 있을 때만 쓴다

`Verification` 필드가 비어 있는 항목에 "완료 · 해결됨 · 끝" 같은 표현을
쓰지 않는다. §9 의 몇 단계인지로 대체한다.

### 감사의 목적

발견을 목록화하는 것이 아니라, **같은 문제가 다시 발생하지 않도록 감시
체계 자체를 강화하는 것**이다. 항목을 닫을 때마다 "이 결함이 재발하면
무엇이 그것을 잡는가" 를 확인한다. 답이 "사람의 주의력" 뿐이면 자동
장치(CI · 검증기 · 스키마 제약)를 `Required Action` 에 추가한다.

---

## 부록 A. 항목 ID 와 Severity (확정)

### ID 규칙

- 형식: `P<severity>-<순번>` (예: `P1-2`)
- **영구 불변 · 재사용 금지.** `FIXED`/`VERIFIED` 후에도 ID 를 유지하고,
  삭제된 항목의 번호를 새 항목에 다시 쓰지 않는다.
- 커밋 메시지 · PR · 후속 감사에서 이 ID 로 참조한다.

### Severity 정의

| 등급 | 정의 | 대응 |
|---|---|---|
| **P0** | 법적·계약적 노출. 제3자 피해 가능 | 다른 작업을 멈추고 처리 |
| **P1** | 데이터 무결성. 조용히 잃거나 되돌릴 수 없음 | 기능 작업보다 우선 |
| **P2** | 검증 체계. 결함을 **발견할 수단**의 부재 | P1 수정의 선행 조건이 되는 경우가 많음 |
| **P3** | 기능 공백. 목표 대비 미구현 | 계획된 순서대로 |
| **P4** | 전략. 목표 자체의 타당성 | 대개 DECISION |

**P2 는 P1 보다 낮은 등급이 아니다.** 검증 수단이 없으면 P1 수정이 맞게
됐는지 확인할 수 없으므로, 실행 순서에서는 P2 가 앞설 수 있다.

## 부록 B. AUDIT.md 항목 템플릿

```markdown
### P1-2 · guide_id Cloud 왕복 소실
- Type       : DEFECT
- Severity   : P1
- Status     : OPEN            ← §2 상태 머신 · §9 단계
- Evidence   : <file:line> — `<기준 커밋 시점 코드 인용>`   ← 동결
- Impact     : <결과 판단>                                  ← 동결
- Required Action : <해야 할 일>
- Dependency : <선행 항목 ID> / 없음
- Verification    : <재실행 가능한 증명 방법> · <결과>
```

## 부록 C. 기각된 가설도 기록한다

감사 중 세웠다가 **검증으로 기각된 가설**을 남긴다. 기록하지 않으면 다음
감사자가 같은 우려를 반복한다.

```markdown
### 기각 · git history 재작성이 engine_version 을 깨뜨린다
- 가설  : filter-repo 가 SHA 를 바꾸면 ocr_corrections.engine_version 의
          참조가 끊긴다 (schema.sql:124 주석 근거)
- 검증  : cloudStore.js:441 확인 — 기록 값은 SHA 가 아닌 고정 문자열
- 결론  : 기각. 단, 검증 과정에서 별도 DEFECT 발견 → P?-? 로 분리
```

이 형식은 "가설 → 검증 → 결론" 을 강제하므로, **기각 과정에서 발견되는
진짜 결함**을 놓치지 않는다.

## 명시적으로 확정되지 않은 것 `[확인 필요]`

- PII 패턴 가드의 정확한 정규식 목록 — 오탐(합성 fixture 의 더미 번호)과
  미탐의 균형이 필요하다 `[확인 필요]`
- 재감사 주기 · 트리거 조건(분기별 · main merge 전 · Scope 추가 시)
  `[확인 필요]`
- `backend/` `frontend/` `mobile/` `shared/` 영역의 감사 여부와 시점
  `[확인 필요]`
- `AUDIT.md` · `AUDIT_PROTOCOL.md` 를 `DEVELOPMENT_RULES.md` §8 "수정 허용
  범위" 문서 트랙에 추가할지 `[확인 필요]`
